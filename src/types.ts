export enum SocketMessagesType {
    CREATE = "create",
    JOIN = "join",
    CHAT = "chat"
}
export enum JOINING_STATUS {
    IDLE = "idle",
    PENDING = "pending",
    SUCCESS = "success",
    FAILED = "failed"
}